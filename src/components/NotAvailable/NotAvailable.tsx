import { Box } from "@mui/material";

export const NotAvailable: React.FC = () => {
  return (
    <Box className="w-full text-center text-(--color-secondary)">
      - NOT AVAILABLE -
    </Box>
  );
};
