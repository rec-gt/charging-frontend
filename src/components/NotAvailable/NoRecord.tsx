import { Box } from "@mui/material";

export const NoRecord: React.FC = () => {
  return (
    <Box className="w-full text-center text-(--color-secondary) text-[10pt]">
      - NO RECORD -
    </Box>
  );
};
